# Simplified requirements that avoid compilation issues
# Use pre-built wheels where possible

# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-dotenv==1.0.0
httpx==0.25.2
openai==1.3.0
python-multipart==0.0.6

# Use pydantic v1 to avoid Rust compilation
pydantic==1.10.13

# Database (SQLite only, no compilation)
sqlalchemy==2.0.23
aiosqlite==0.19.0

# HTML processing (no lxml)
beautifulsoup4==4.12.2
html2text==2020.1.16

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0

# Linting
ruff==0.1.6
mypy==1.7.0